FROM node:22-alpine

WORKDIR /app

# Pre-install dependencies — only re-runs when package.json/lock changes
COPY package.json package-lock.json* ./
RUN npm install

EXPOSE 5173

CMD ["npm", "run", "dev"]
